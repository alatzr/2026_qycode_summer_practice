# CF1106D Lunar New Year and a Wander 题解

已访问的点形成一个连通区域。下一次首次访问的点可以是这个区域所有未访问邻居中的任意一个；为了让记录序列字典序最小，每一步都选其中编号最小的点。

用小根堆维护当前边界：起点 1 入堆，取出最小点后，把所有尚未入堆的邻居加入。邻居在入堆时立刻标记，重复边和自环就不会造成重复记录。

复杂度：每个点和每条边至多处理一次，时间 `O((n+m) log n)`，空间 `O(n+m)`。
