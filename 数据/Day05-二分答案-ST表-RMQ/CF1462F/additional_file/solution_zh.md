# CF1462F The Treasure of The Segments 题解

枚举保留的中心线段 `[l,r]`。与它不相交的线段只可能完全在左边（右端点 `< l`）或完全在右边（左端点 `> r`）。

分别排序所有左端点、右端点后，二分统计这两类线段的数量，和就是以该线段为中心时要删除的数量，取最小值。

复杂度：每组排序 `O(n log n)`，枚举和二分 `O(n log n)`。
